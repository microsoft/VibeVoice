# Chunking Module

This package provides heading-based markdown chunking and a chunked TTS processor.

Key entries:
- `MarkdownChunker` for parsing markdown headings into chunks.
- `ChunkedTTSProcessor` for synthesizing each chunk and merging audio.
